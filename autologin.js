var id = document.getElementsByName("ACCOUNTUID")[0];
if(id){
    id.value = "YOUR ID";
}

var passwd = document.getElementsByName("PASSWORD")[0];
if(passwd){
    passwd.value = "YOUR PASSWORD";
}

var btn = document.getElementsByName("SUBMIT")[0];
if(btn){
    btn.click();
}
